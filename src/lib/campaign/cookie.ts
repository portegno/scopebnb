/**
 * The name and life of the campaign cookie, on its own so the edge middleware
 * can import it without dragging in the Admin SDK.
 */
export const COOKIE = "sb_l";

/**
 * Ninety days.
 *
 * A club does not decide on the visit: it takes the idea to a meeting and comes
 * back a week or three later, sometimes from the observatory's other computer.
 * Forwarding the code on internal links only covers the session, and the
 * session is not where a committee makes up its mind.
 */
export const DIAS = 90;
